#include <iostream>
using namespace std;

const int maxn = 2e5 + 10;
int a[maxn];

int main()
{
    int t;
    cin >> t;
    while (t--)
    {
        int n, A, B;
        cin >> n >> A >> B;
        for (int i = 1; i <= n; i++)
        {
            cin >> a[i];
        }
        
    }
    return 0;
}